<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Confirm Add Student</title>
  <link rel="stylesheet" href="/css/confirm_add_group.css">
</head>
<body>
  <header>
    <div class="logo-container">
        <img src="/images/IIITP_Logo.jpeg" alt="IIITP Logo" class="logo-img">
        <h1>Student Project Management</h1>
    </div>
    <nav>
        <a href="/admin/dashboard">Admin</a>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
        <a href="/admin/login">Logout</a>
    </nav>
</header>

  <main>
    <section class="confirmation-box">
      <h2>Student Already Assigned</h2>
      <p>
        The student <strong><%= student.first_name %> <%= student.last_name %></strong> (MIS: <%= student.mis_number %>) 
        is already a member of <strong>Group <%= student.group_id %></strong>.
      </p>
      <p>Do you want to remove the student from the previous group and add them to <strong>Group <%= targetGroupId %></strong>?</p>

      <form method="POST" action="/admin/manage-group/<%= targetGroupId %>/confirm-add">
        <input type="hidden" name="student_id" value="<%= student.student_id %>">
        <input type="hidden" name="groupId" value="<%= targetGroupId %>">
        <input type="hidden" name="currentGroup" value="<%= student.group_id %>">
        <button type="submit" class="confirm-button">Yes, Move Student</button>
      </form>

      <form method="GET" action="/admin/manage-group/<%= targetGroupId %>">
        <button type="submit" class="cancel-button">Cancel</button>
      </form>
    </section>
  </main>
</body>
</html>
