<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/faculty_change_password.css">

    <title>Document</title>
</head>
<body>

    <header>
        <div class="logo-container">
          <img src="/images/IIITP_Logo.jpeg" alt="IIITP Logo" class="logo-img">
          <h1>Student Project Management</h1>
        </div>
      
        <nav style="display: flex; align-items: center; gap: 1rem;">
          <a href="/">Home</a>
          <a href="/about">About</a>
          <a href="/contact">Contact</a>
          <a href="/faculty/login">Logout</a>
      
          <% if (faculty) { %>
            <a href="/faculty/<%= faculty.faculty_id %>/profile" title="<%= faculty.first_name %>" style="background-color: #007bff; color: white; border-radius: 50%; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; font-weight: bold;">
              <%= faculty.first_name.charAt(0).toUpperCase() %>
            </a>
          <% } %>
        </nav>
      </header>

    <div class="container">
        <% if (alert) { %>
            <script>alert("<%= alert %>")</script>
        <% } %>

        <h2>Change Password</h2>
        <form method="POST" action="/faculty/<%= faculty_id %>/change-password">
            <label>Old Password: <input type="password" name="old_password" required /></label><br>
            <label>New Password: <input type="password" name="new_password" required /></label><br>
            <label>Confirm New Password: <input type="password" name="confirm_password" required /></label><br>

            <button type="submit">Change Password</button>
        </form>
    </div>

</body>
</html>